<h1>ルート兼、投稿一覧表示画面</h1>

<h1>ランダムに投稿をシャッフルするボタンをつける<h1>

<%= link_to "新規投稿", new_post_path %>

<% @posts.each do |post| %>

<br>
<br>
<br>
<br>

          <%= link_to post_path(post) do  %>
             <%= attachment_image_tag post, :post_image, :fill, 100,100, format:"", fallback:"" %>
          <% end %>
      <table>
      <tr>
       <td>いいねの数表示</td>
       </tr>
       <tr>
       <td>コメント<%= post.post_comments.count %>件</td>
       </tr>
    <p></p>
    <p>BAの有無表示</p>
</tr>
</table>

<% end %>