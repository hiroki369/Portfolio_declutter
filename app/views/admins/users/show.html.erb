アドミン用ユーザー詳細表示ページ
<h1>編集</h1>

<!-- アップデートのコントローラーを呼び出してしまっている -->
<% if @user.deleted? %>
<%= form_for(@user, url: {controller: "admins/users", action: "restore"}) do |f| %>
<%= f.hidden_field :user, :value => @user %>
<%= f.submit "ユーザー復元する", data: {confirm: "本当に復元しますか？"}  %>
<% end %>

<%= link_to "ユーザーを復元する", {controller: :users, action: :restore, method: :patch}, data: { confirm: '本当に復元しますか？'}  %>
<% end %>


<br>
<%= link_to "ユーザー登録情報を編集する", admins_users_edit_path(@user) %>

<br>
<%= attachment_image_tag @user, :profile_image, :fill, 100,100, format:"", fallback:"noimage.jpg", size:'100x100' %><br>

<li>ユーザーネーム
<%= @user.name %>
</li>
<li>
自己紹介
<%= @user.introduction %><br>
</li>

<% if @user == current_user %>
<%= link_to "登録情報編集", edit_user_path(@user)  %>
<h4>退会するボタン作る</h4>
<% end %>




<h1>自分の投稿一覧</h1>
<% @user.posts.each do |post| %>

<li>
	<%= link_to admins_posts_show_path(post) do %>
		<%= attachment_image_tag post, :post_image, :fill, 100,100, format:"", fallback:"noimage.jpg", size:'100x100' %>
	<% end %>
		<%= post.title %>
		<%= post.body %>
ベストアンサーカラムがtrueの場合、ベストアンサー認定済表示出す！
</li>

<% end %>